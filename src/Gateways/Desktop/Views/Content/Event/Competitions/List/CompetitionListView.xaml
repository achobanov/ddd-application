<UserControl x:Class="EnduranceJudge.Gateways.Desktop.Views.Content.Event.Competitions.List.CompetitionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:content="clr-namespace:EnduranceJudge.Gateways.Desktop.Views.Content.Event"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <StackPanel Style="{StaticResource Wrapper}">
        <Button Style="{StaticResource Button}"
                Command="{Binding ChangeToCreate}"
                Content="{x:Static content:EventPageContent.CreateButtonText}" />

        <ListBox Style="{StaticResource List}"
                 ItemsSource="{Binding Competitions}" />

    </StackPanel>
</UserControl>
