<UserControl x:Class="EnduranceJudge.Gateways.Desktop.Views.Content.Event.NavigationStrip.EventNavigationStripView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:EnduranceJudge.Gateways.Desktop.Views.Content.Event.NavigationStrip"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <StackPanel Style="{StaticResource Navigation}">
        <Button Style="{StaticResource Button}"
                Command="{Binding ChangeToEventsList}"
                Content="{x:Static local:EventNavigationStripContent.EnduranceEvents}" />

        <Button Style="{StaticResource Button}"
                Command="{Binding ChangeToCompetitionsList}"
                Content="{x:Static local:EventNavigationStripContent.Competitions}"></Button>

    </StackPanel>
</UserControl>
