<UserControl x:Class="EnduranceJudge.Gateways.Desktop.Components.Content.Import.Import"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             HorizontalContentAlignment="Stretch">

    <StackPanel Background="Aqua">
        <DockPanel Style="{StaticResource Form-Item-Primary}" Background="Brown">

            <Button Style="{StaticResource Button-Form-Item}"
                    Command="{Binding OpenFolderDialog}"
                    Name="Select_WorkDirectory">Select Work Directory</Button>

            <TextBox Style="{StaticResource Input-Primary}"
                     Text="{Binding WorkDirectoryPath, Mode=OneWay}"
                     IsReadOnly="True"/>
        </DockPanel>

        <DockPanel Style="{StaticResource Form-Item-Primary}"
                   Visibility="{Binding ImportVisibility}">

            <Button Style="{StaticResource Button-Primary}"
                    Command="{Binding OpenImportFileDialog}"
                    Name="Select_ImportFile">Select Import File</Button>

            <TextBox Style="{StaticResource Input-Primary}"
                     Text="{Binding ImportFilePath, Mode=OneWay}"
                     IsReadOnly="True"/>
        </DockPanel>
    </StackPanel>
</UserControl>
